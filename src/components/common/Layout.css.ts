import { style } from '@vanilla-extract/css';

const layout = style({
  display: 'flex',
  flexDirection: 'column',
  width: '1000px',
  minHeight: '100%',
  margin: '0 auto',
});

export default layout;
